import React, { Fragment } from "react";
import TaskForm from "./TaskForm";

const NewTask = (props) => {
  return (
    <Fragment>
      <TaskForm />
    </Fragment>
  );
};

export default NewTask;
